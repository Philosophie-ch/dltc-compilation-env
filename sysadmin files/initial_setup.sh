#!/usr/bin/env bash


# TODO: disallow root access

# TODO: create 'copyeditor' sudoer user and group


# TODO: install docker

# TODO: put copyeditor in the docker group


# TODO: docker postinstall config to use 'docker' without sudo


# TODO: put the public keys of the team in copyeditor's home, so the container can grab them


# TODO: useful cronjobs (backup, updates, monitoring send to the droplet's admin via email, etc)